# Frontend environment configuration
# Copy this file to .env.production (or .env.local for local testing)
# and update values for your deployment.

# Enforce Google-certified CMP signal before serving ads.
# Set to true in production if you serve EEA/UK/CH traffic.
REACT_APP_REQUIRE_GOOGLE_CMP=true
